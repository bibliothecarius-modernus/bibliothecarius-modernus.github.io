{
  "name": "bibliothecarius-modernus",
  "version": "1.0.0",
  "private": true,
  "description": "Search index builder for Bibliothecarius Modernus",
  "type": "module",
  "scripts": {
    "build:search": "node scripts/build-search-index.mjs",
    "build": "bundle exec jekyll build && npm run build:search"
  },
  "dependencies": {
    "pagefind": "^1.3.0",
    "yaml": "^2.7.0"
  }
}
